import 'package:flutter/material.dart';

class ImageDisplay extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey, // 임시 배경색 (Firebase 이미지로 대체 예정)
      child: Center(
        child: Text("Image Display"), // 임시 텍스트
      ),
    );
  }
}
